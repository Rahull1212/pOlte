/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  eslint: {
    // No ESLint config is set up yet in this scaffold; don't block builds on it.
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
